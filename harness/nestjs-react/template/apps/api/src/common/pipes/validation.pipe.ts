// Re-export for convenience — the global pipe is configured in main.ts
// Individual controllers can import and use ValidationPipe directly if needed

export { ValidationPipe } from '@nestjs/common';
