/**
 * Factory barrel export.
 *
 * Add domain-specific factories as Prisma models are created.
 * Each model gets a `build<Model>()` function returning a typed object.
 * See conventions/testing.md → Test Factories section.
 *
 * Example:
 *   export * from './user.factory';
 *   export * from './project.factory';
 */
