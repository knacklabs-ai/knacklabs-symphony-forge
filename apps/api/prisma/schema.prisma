generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ── Harness Placeholder ──────────────────────────────────────────────
// This schema ships empty. The agent generates models from BRIEF.md
// domain concepts + convention rules (database.md).
//
// Convention reminders:
//   - UUIDv7 PKs: @id @default(dbgenerated("gen_random_uuid()"))
//   - snake_case table/column names via @@map / @map
//   - Required: id, created_at, updated_at on every model
//   - Soft deletes: deleted_at DateTime? where applicable
//   - See conventions/database.md for full rules
